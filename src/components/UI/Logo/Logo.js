import React from 'react';

import classes from './Logo.module.scss';

const Logo = () => {
    return (
        <span className={classes.Logo}>
            &nbsp;
        </span>
    )
}

export default Logo;